{
    "private": true,
    "name": "j1-nbinteract",
    "version": "1.0.7",
    "description": "Export interactive HTML pages from Jupyter Notebooks for J1 Template",
    "scripts": {
        "build": "run-s -s build:*",
        "build:msg_start": "echo Build project ..",
        "build:py": "echo .. build python backend files && npm run --silent clean:py && python setup.py bdist_wheel > build-backend.log && echo .. finished.",
        "build:js": "echo .. build javascript frontend files && lerna run --scope j1-nbinteract-core build > build-frontend.log && echo .. finished.",
        "build:msg_final": "echo .. building project finished.",
        "clean": "run-p -s clean:*",
        "clean:start": "echo Clean up project root files ..",
        "clean:py": "shx rm -rf dist && shx rm -rf build && shx rm -rf j1_nbinteract.egg-info",
        "clean:locks": "shx rm -f *.lock && shx rm -f package-lock.json",
        "clean:logs": "shx rm -f *.log && shx rm -f stats.json",
        "clean:packages": "echo Clean up project packages .. && lerna run --parallel clean",
        "install--local": "pip install -e . 2>&1 > install--local.log && shx tail -n 7 install--local.log",
        "publish--pypi": "twine upload dist/*",
        "publish--npm": "cd ./packages/j1-nbinteract-core && npm publish",
        "reset": "run-s -s reset:*",
        "reset:start": "echo Reset development system to factory state ..",
        "reset:clean-files": "npm --silent run clean",
        "reset:clean": "echo Remove js modules from all packages .. && lerna clean --yes",
        "reset:done": "echo done.",
        "setup": "npm --silent run setup-start && npm --silent run setup-base-nodejs && run-s -s setup:*",
        "setup-start": "echo Set up development system for first use ..",
        "setup-base-nodejs": "echo Bootstrap NodeJS base modules .. && echo J1 Project Base modules: > install.log && npm install --silent rimraf shx npm-run-all cross-var cross-env >> install.log && echo done.",
        "setup-base:py": "echo Bootstrap Python base modules .. && python -m pip install twine && echo done.",
        "setup:node-modules": "echo Bootstrap NodeJS project modules .. && echo J1 Project modules: >> install.log && npm install --silent >> install.log && echo done.",
        "setup:lerna": "echo Initialize NodeJS modules for all development packages .. && lerna init && lerna bootstrap && echo done.",
        "postsetup": "run-s -s patch:*",
        "patch:pre-msg": "echo Processing required patches for the development system ..",
        "patch:d3": "echo .. apply required d3.js patches for nbinterct-core && shx rm -f ./packages/j1-nbinteract-core/node_modules/d3/d3.js && shx cp ./packages/j1-nbinteract-core/patches/d3.fake.errors.js ./packages/j1-nbinteract-core/node_modules/d3/d3.js",
        "patch:post-msg": "echo Processing required patches done."
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/jekyll-one/j1-nbinteract.git"
    },
    "keywords": [
        "J1 Template",
        "Jekyll",
        "jupyter",
        "notebook",
        "python"
    ],
    "devDependencies": {
        "@babel/core": "^7.0.0-beta.39",
        "@babel/plugin-syntax-dynamic-import": "^7.0.0-beta.39",
        "@babel/polyfill": "^7.0.0-beta.39",
        "@babel/preset-env": "^7.0.0-beta.39",
        "@types/requirejs": "^2.1.28",
        "babel-core": "^6.26.0",
        "babel-loader": "8.0.0-beta.2",
        "babel-runtime": "^6.26.0",
        "clean-webpack-plugin": "^4.0.0",
        "css-loader": "^0.28.4",
        "file-loader": "^0.11.2",
        "fs-extra": "^0.30.0",
        "json-loader": "^0.5.4",
        "postcss": "^6.0.2",
        "prettier": "^1.6.1",
        "schema-utils": "^0.4.3",
        "style-loader": "^0.18.1",
        "url-loader": "^0.5.9",
        "webpack": "^4.0.0",
        "webpack-bundle-analyzer": "^2.9.2",
        "webpack-cli": "^3.1.2",
        "webpack-dev-server": "^3.1.1",
        "webpack-merge": "^4.1.1"
    },
    "dependencies": {
        "core-js": "^2.6.12",
        "cross-env": "^7.0.3",
        "cross-var": "^1.1.0",
        "lerna": "^5.1.4",
        "npm-run-all": "^4.1.5",
        "rimraf": "^3.0.2",
        "shx": "^0.3.4"
    }
}
